using System.Collections.Generic;

namespace ConsoleDrawer {
    /* 
    public static class ListaExtensions {
        public static T GetFirst<T>(this IEnumerable<T> lista) {
            if (lista == null) {
                return default(T);
            }
            foreach (var item in lista) {
                return item;
            }

            return default(T);
        }
    }

    */
}